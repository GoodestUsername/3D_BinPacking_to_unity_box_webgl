//Temporary JS Library for Unity to allow data transmission.
mergeInto(LibraryManager.library, {

  ReturnID: function (id) {
      window.alert(id)
  }

  ReturnCoordinates: function(coorarray) {
      coorarray.forEach(concatstring)
  }

});